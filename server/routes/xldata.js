const express = require('express');
const router  = express.Router();

// const users_api = require('//path to our excel data controller');

router.get('/', uploadController.findAll);

router.get('/', uploadController.findAll);

router.get('/', uploadController.findAll);

router.get('/', uploadController.findAll);

router.get('/', uploadController.findAll);

module.exports = router;